# 饮品热点雷达：源码分析与复刻方案

检查日期：2026-09-17。对象：https://valuerdrink.netlify.app/?module=联名&tag=peripheral:玩偶

## 核心结论

这是一个由预处理数据驱动的静态行业周报网站。公开前端使用原生 HTML、CSS、JavaScript，没有发现 React、Vue、Next.js、前端包加载器或图表库。数据来源描述明确指向 Excel，而不是猜测。打开页面时无需实时访问小红书。

应区分两件事：可以确认“展示数据经过 Excel 大宽表生成”，但无法确认“Excel 之前由谁、用什么工具采集”。后台可能存在采集脚本、第三方数据服务或人工整理；公开资源没有提供这些系统的源码。

本次下载并分析了网站公开的 HTML、三个 JavaScript 文件和 CSS，重新计算了原始点赞总数。浏览器页面加载超时，因此没有完成实时视觉、点击交互及网络面板验证。下面关于页面行为的判断来自代码阅读，不冒充运行时测试。

## 1. 公开文件与技术栈

| 资源 | 大小（未压缩文件字节） | 职责 |
|---|---:|---|
| index.html | 15,242 | 页面骨架、导航、报告分区、筛选面板、详情抽屉 |
| styles.css | 51,786 | 色彩变量、布局、响应式与组件样式 |
| app.js | 41,227 | 图表渲染、标签聚合、搜索筛选、排序、抽屉、导出与分享状态 |
| data/report-data.js | 283,582 | 整份周报的帖子、品牌动作、指标与编辑性结论 |
| xlsx-export.js | 12,205 | 在浏览器生成真正的 XLSX 文件 |

HTML 依次加载数据、导出工具、应用脚本。数据声明为 `window.DRINK_RADAR_DATA = {...}`，应用通过这个全局变量取数据，使用 Map 建立帖子和动作索引。

公开应用脚本没有 fetch、XMLHttpRequest 或 axios 调用。筛选、排序与图表聚合都在浏览器内完成。“打开原帖”只是跳到小红书链接，不是后台获取帖子内容。

资源 URL 带有 `?v=` 版本号，用于缓存更新。部署域名来自 Netlify，但域名不能证明其内部使用了哪种 CI、仓库或数据采集后端。

柱状图由 DOM 和 CSS 宽度生成；没有必要为了复刻这些图表安装 ECharts。XLSX 导出代码自行实现 XML、CRC32 和 ZIP 组装；我们无需复制这套实现，初期 CSV 导出即可，必须交付 XLSX 时再选择成熟库。

源码证据：[HTML](https://valuerdrink.netlify.app/)、[应用逻辑](https://valuerdrink.netlify.app/app.js)、[数据文件](https://valuerdrink.netlify.app/data/report-data.js)、[样式](https://valuerdrink.netlify.app/styles.css)、[导出工具](https://valuerdrink.netlify.app/xlsx-export.js)。本地原始快照保存在 evidence/。

## 2. Excel 来源是明确证据

数据的 `meta.source` 记录：

```text
workbook: 02_数据处理/饮品热点雷达_分析标签大宽表_2026W32.xlsx
worksheet: 分析标签大宽表
headerRow: 4
```

其他元信息：

- 报告观察期：2026-08-03 至 2026-08-09，即 2026W32。
- 数据文件生成时间：2026-08-19T08:47:19.455Z。
- 点赞采集区间：8 月 18 日 15:42:11 至 16:18:00，UTC+8。
- 收藏与评论截点分布在 8 月 14 日至 18 日，未与点赞完全同步。
- 8 个外部账号组成观察池；纳入行业资讯、美食达人，排除品牌官方账号。
- 数据声明当前为 AI 文本复核版，关键标签仍需业务确认。

因此，它是“观察某周发布的内容，再在稍后统一刷新点赞”的报告，不是实时榜单。网页中的结论、解释、标签已在数据文件中准备好，也没有前端在线调用大模型的证据。

最符合现有证据的流程：

```text
帖子采集或人工整理（具体工具未知）
    → Excel 分析标签大宽表（明确存在）
    → 清洗、标签复核、品牌动作归并、指标聚合
    → 生成 report-data.js（明确存在）
    → 与静态页面一起部署
```

Excel 原文件、转换脚本、采集脚本、真实后台与 Netlify 项目设置均未取得，不能声称已经还原。

## 3. 真正值得复用的数据结构

顶层包含六部分：

| 对象 | 内容 |
|---|---|
| meta | 观察周期、数据来源、截点、统计口径、标签规则、限制 |
| summary | 总体和各模块的统计 |
| editorial | 总览、模块结论、图表解释等预先写好的文案 |
| brandActions | 按同一品牌市场动作归并的实体 |
| notes | 帖子原始文本、账号、原帖链接、指标及细分标签 |
| unassignedNotes | 尚未归并到动作的内容 |

关键关系是 **一个品牌动作对应多篇帖子**。例如一次品牌联名可能被两个行业账号、一个达人各发一篇，网站把这些帖子归并成一个案例。

帖子实体保存发布日期、标题、正文、话题、作者、点赞/收藏/评论、采集时间、原帖链接，以及产品、联名、活动等分析标签。品牌动作保存动作 ID、品牌、动作名称、相关帖子 ID、主分类、多模块观察标签、总体指标、模块内指标和案例解释。

业务标签细于普通热搜：

- 新品：饮品品类、上新/回归、原料、风味、双果复配等。
- 联名：IP/品牌关系、对象、IP 类型、杯套纸袋等包材、玩偶挂件等周边。
- 活动：互动玩法、场地、参与方式、奖励、买赠/买一赠一等优惠。

这层结构才是复刻重点。只安装一个热榜网站，无法自动得到这些标签及动作归并关系。

## 4. 数字与统计口径

本地重新求和确认：52 篇帖子，29 个品牌动作，总点赞 33,288。数据声明 48 篇已映射到动作，4 篇未归并。

统计定义：热度只使用点赞；单篇达到 1,000 赞定义为千赞内容。收藏、评论保留为明细，不进入排行。

新品、联名、活动允许同时标注在一篇帖子或一个动作上，所以模块数量、点赞不能直接相加。品牌动作另有互斥主分类，默认优先级为联名 > 新品 > 活动，允许人工覆盖。

页面模块榜使用 `moduleMetrics`，动作详情与动作库主要使用动作总体 `metrics`。这能解释“某个案例详情里的点赞比分类榜里多”：详情列出该动作全部相关帖子，分类榜只算符合该模块的帖子。

样本是固定账号池，不能把这些份额当作小红书全平台市场份额，也不能用点赞替代销量、曝光或营销 ROI。

## 5. 页面结构、视觉与交互

内容顺序：顶部品牌导航 → 周期和总览结论 → 三类信号卡 → 前五动作榜 → 新品分析 → 联名分析 → 活动分析 → 可筛选品牌动作库 → 服务商信息。

视觉采用米白背景 `#f5f3ee`，深色文字和重点榜单，新品黄绿色 `#d8ee66`、联名浅蓝 `#b9d9ff`、活动橙粉 `#ffb69b`。使用系统字体、大圆角卡片、CSS Grid，最大内容宽度约 1,240px；存在多处响应式断点。

交互包括：点击图表标签进入筛选、按品牌/模块/千赞状态筛选、关键词搜索、按总赞/千赞数/均赞/发布时间排序、分批显示、详情抽屉、复制和打开原帖、当前结果 Excel 导出。抽屉和移动筛选面板有键盘处理、焦点恢复与 ARIA 标记。

URL 参数用于保存状态：`action`、`brand`、`module`、`hit`、`q`、`tag`。用户提供的链接表示“联名 + 周边标签包含玩偶”。在数据快照中命中 Pingu 动作，关联两篇帖子。URL 的 module 不是单独页面，也不是后端接口。

### 复刻时可以改善的地方

1. **明确搜索范围。** 实际动作筛选调用 `actionSearchText`，搜索动作名称和聚合标签；虽然文件定义了 `noteSearchText`，但没有看到调用，不能称为帖子正文全文搜索。
2. **明确导出范围。** `exportCurrent` 导出命中动作的全部帖子，不会逐篇再按当前模块或周边标签筛选。这与“严格只导出玩偶帖”并非同一需求。
3. **统一周期配置。** 页脚、导出文件名等位置含硬编码 2026W32，应统一由元信息生成，避免换周后遗漏。
4. **保留原始快照和截点。** 更新指标不要覆盖历史采集结果，否则无法分析真实变化。
5. **显式保存复核状态和证据。** AI 标签可以辅助，但跨帖子归并、复合营销动作需要人工确认。
6. **控制公开数据内容。** 原站把正文和含 xsec 参数的原帖链接放入公开 JS。我们的公开报告只输出展示所需字段，原始数据另存；xsec 参数不能直接认定为账号登录凭据。
7. **先用正确的简单实现。** 这里不到百篇帖子，没有必要安装搜索服务、消息队列或重型后台框架。

## 6. GitHub 项目筛选

| 项目 | 适用部分 | 是否适合当前作为整站底座 |
|---|---|---|
| [TrendRadar](https://github.com/sansan0/TrendRadar) | 热榜/RSS、关键词筛选、AI 分析、推送 | 不优先：适合监控线索，不自带本需求的帖子标签和动作归并；LICENSE 为 GPL-3.0 |
| [NewsNow](https://github.com/newsnext/newsnow) | 多来源资讯聚合、热点阅读界面 | 不优先：MIT，但当前依赖 React/TanStack 等一整套工具，需要新增品牌动作模型与导入流程 |
| [MediaCrawler](https://github.com/NanmiCoder/MediaCrawler) | 小红书关键词、指定帖子、指定作者采集；多种存储输出 | 采集研究参考；仓库 README 明确限制商业用途，不直接作为商业产品采集底座安装 |
| [PapaParse](https://github.com/mholt/PapaParse) | 成熟 CSV 解析、引用/换行/分隔符处理 | 直接采用：MIT、无运行时依赖，匹配 Excel 另存 CSV 的导入路径 |

许可来源：[TrendRadar LICENSE](https://github.com/sansan0/TrendRadar/blob/master/LICENSE)、[NewsNow LICENSE](https://github.com/newsnext/newsnow/blob/main/LICENSE)、[MediaCrawler README](https://github.com/NanmiCoder/MediaCrawler)、[PapaParse LICENSE](https://github.com/mholt/PapaParse/blob/master/LICENSE)。

本轮检索没有确认到可直接完成“饮品行业标签 + 品牌动作归并 + 周报解释”的完整开源成品。热榜项目可以作为未来的线索入口，不能替代这个行业数据处理流程。

按 ponytail 的最小方案，实际安装了 PapaParse，并编写薄薄一层数据适配。没有修改第三方库自身，也没有把大而不匹配的整站框架搬进项目。版本由 package-lock.json 锁定。

## 7. 已落地的最小改造

`tools/import-radar.cjs` 接受统一格式的 UTF-8 CSV，输出 JSON：

- 校验必需列、非负整数点赞、合法日期、带时区的指标截点、模块与链接协议。
- 重复帖子 ID 直接报错，防止无声重复统计。
- 按 action_id 归并帖子，同一动作 ID 的品牌或名称冲突报错。
- 输出总指标、动作指标、多模块指标、周边标签及帖子关联。
- 临时文件生成成功后再替换目标文件，避免校验失败覆盖既有报告。

`examples/notes.csv` 全部为明确虚构的示例。运行 `rtk npm run demo` 可以看到三篇示例帖子归并成两个动作，共 2,300 赞。`rtk npm run check` 已通过归并、多标签重叠统计、CSV 引用、重复 ID、非法日期、非法点赞和动作冲突检查。

工具使用我们自己的 `radar-kit-0.1` 精简 schema，不是原站完整 schema，也不能直接替换原站 report-data.js。它没有采集小红书、生成 AI 结论、直接读取 XLSX 或提供成品网页；Excel 首先另存为 CSV UTF-8。这是把最重要的数据路径做成可执行基础，而非声称整站已经完成。

## 8. 如果复刻，我们实际需要做什么

第一阶段：锁定数据口径与 Excel 模板。确定账号观察池、周期、品牌动作 ID、模块标签、指标截点、复核人；取一周真实数据验证去重和归并。没有真实样本时，外观做好仍然没有可信报告。

第二阶段：生成静态周报。用目前的 JSON 流程添加产品、IP、优惠等必要字段；输出统计和每条结论的证据；做原生 HTML/CSS/JS 页面，包含总览、模块榜、动作库、详情、URL 筛选和导出。保持自有品牌与文案。原站公开资源仅作为研究快照，不直接作为可再分发模板。

第三阶段：稳定更新。保留每周 JSON 和原始表快照，增加历史周选择；在构建时生成多个周报页。只需要定期人工导入时，数据库和在线后台都可暂缓。

第四阶段：若确实需要自动采集，再独立接入授权的数据服务或已确认可使用的采集工具。采集输出进入同一模板，清洗与复核后生成报告。不要让访问者的浏览器直接调用小红书；登录态、限流、采集失败与链接有效期应由独立采集流程处理。

多人编辑、较大历史数据或频繁指标更新时，再加入 SQLite、轻量管理界面和增量更新；超出单机需求后才考虑 PostgreSQL。AI 的职责是建议标签、提供证据摘录与写分析草稿，统计值仍由确定性代码计算。

目前最短路径：**Excel/CSV → 校验及动作归并 → JSON 周报 → 静态网站**。把抓取能力和报告展示拆开实施，已有数据即可完成第一版，不必等自动采集系统。
