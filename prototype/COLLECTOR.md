# 本地采集工作区

运行 `npm start`，打开 http://127.0.0.1:4173 的“数据采集”。采集界面不再嵌入或遥控浏览器。

在 Chrome/Edge 扩展管理页启用开发者模式，加载项目根目录下的 `extension` 文件夹。随后在普通浏览器中打开一篇小红书笔记，点击“Signal 采集助手”打开常驻侧边栏，再点击“采集当前页”把当前页面可见字段放入本地队列。

扩展不读取 Cookie，也不执行批量访问。队列可以批量同步到工作台，也可以从侧边栏导出 CSV。未安装扩展时可以载入虚构样本，或粘贴采集 JSON 演示后续复核流程。
