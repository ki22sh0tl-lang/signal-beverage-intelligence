services:
  - type: web
    name: signal-beverage-intelligence
    runtime: node
    plan: free
    buildCommand: npm ci
    startCommand: npm start
    healthCheckPath: /healthz
    envVars:
      - key: NODE_VERSION
        value: 22
      - key: CAPTURE_TOKEN
        generateValue: true
