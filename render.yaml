services:
  - type: web
    name: signal-drink-radar
    runtime: node
    plan: free
    buildCommand: npm ci
    startCommand: npm start
    healthCheckPath: /healthz
    envVars:
      - key: NODE_VERSION
        value: 22
      - key: CAPTURE_TOKEN
        generateValue: true
